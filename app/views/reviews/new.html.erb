<h1>Ajouter un avis</h1>

<%= simple_form_for [ @restaurant, @review ] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit "Ajouter un avis", class: "btn btn-primary"%>
<% end %>
